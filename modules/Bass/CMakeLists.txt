# Dine Bass: AU + Standalone built from the shared channel-plugin base (modules/Common).
dine_add_channel_product(LiveMixBass
    PRODUCT Bass
    NAME "Dine Bass"
    BUNDLE_ID "com.dine.bass"
    PLUGIN_CODE Lmba
    SOURCES BassProcessor.cpp)
