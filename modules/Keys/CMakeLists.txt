# Dine Keys: AU + Standalone built from the shared channel-plugin base (modules/Common).
dine_add_channel_product(LiveMixKeys
    PRODUCT Keys
    NAME "Dine Keys"
    BUNDLE_ID "com.dine.keys"
    PLUGIN_CODE Lmky
    SOURCES KeysProcessor.cpp)
